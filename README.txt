запускать app/start.py

в качестве working directory в pycharm установить корень проекта

сделать exe одной командой:
из корня проекта в консоли 'pyinstaller .\build.spec'
должен быть установлен pyinstaller последней версии

после установки внутри не будет config-файла, поэтому я его вручную копирую config/config1.ini (вместе с папкой, но все лишнее убрав)